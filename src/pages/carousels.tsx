import type { FC } from 'react';
import CarouselsPage from '../components/page/carousels.page';

const Carousels: FC = () => (
  <div className="bg-slate-500 py-16">
    <CarouselsPage />
  </div>
);

export default Carousels;
